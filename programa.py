def calcular_area(base, altura):
    return (base * altura) / 2


def pedir_lados():
    base = float(input("Ingresar la longitud: "))
    altura = float(input("Ingresar la altura: "))
    return base, altura
